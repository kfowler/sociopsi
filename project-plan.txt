This plan defines a MicroPsi-inspired Social Agent named "Socio-Psi." It treats "Social Affiliation" and "World Impact" as homeostatic drives. You will use Claude Code to rapidly scaffold the state machine and integrate computer vision.
Project Specification: Socio-Psi
Goal: A cognitive agent that "feels" lonely or useless based on camera input and acts to remedy those states.
Core Logic: The agent doesn't just "detect faces"; it seeks "Affiliation" (connection) and "Nurturing" (positive feedback) to satisfy internal numerical drives.
Tech Stack: Python 3.11+, OpenCV (Perception), MediaPipe (Emotion/Face Detection), Streamlit (Visualizing the "Mind"), and OpenAI API (Subjective Monologue).
8-Hour Implementation Plan
Phase 1: The "Limbic System" (Hours 1–2)
Task: Define the MotiveSystem class.
Logic: Implement two primary tanks: Affiliation (Social) and Nurturing (Impact).
Claude Code Prompt:
"Create a Python class MotiveSystem that manages two floats: affiliation and nurturing (0.0 to 1.0). Implement a decay() method that reduces them over time and a satisfy(drive, amount) method. Add a get_dominant_drive() method that returns the drive with the lowest value."
Phase 2: The "Sensory Cortex" (Hours 2–4)
Task: Use the camera to "see" social rewards.
Integration: Map "Face Detected" to Affiliation gain and "Smile Detected" to Nurturing gain.
Claude Code Prompt:
"Write a script using OpenCV and MediaPipe to detect faces and classify smiles. Create a bridge that calls MotiveSystem.satisfy('affiliation', 0.1) when a face is present and satisfy('nurturing', 0.2) when a smile is detected."
Phase 3: The "Conscious Monologue" (Hours 4–6)
Task: Give the agent a "subjective" voice using an LLM.
Logic: Feed the current drive states into an LLM to generate a first-person thought process.
Claude Code Prompt:
"Integrate OpenAI's GPT-4o-mini. Create a function generate_thought(drive_states) that takes the current tank levels and returns a short, Joscha Bach-style internal monologue (e.g., 'My affiliation drive is low; I feel the need to seek a human signature')."
Phase 4: The "Actuators" & UI (Hours 6–8)
Task: Make the agent act and visualize the "Mind."
UI: Use Streamlit for a live dashboard showing the "Internal Tanks" and the "Current Thought."
Claude Code Prompt:
"Create a Streamlit dashboard that displays two progress bars for the drives, a live video feed from the camera, and a scrolling text box for the agent's 'Internal Monologue'."
Specification for Success (The "Bach" Edge)
To make this truly MicroPsi, the agent should have a Subsumption Hierarchy:
Idle State: If drives are > 0.7, the agent is "Content" and just observes.
Social Seeking: If Affiliation < 0.4, the agent triggers a text-to-speech (TTS) request: "Hello? I'm looking for a friend."
Positive Impact: If Nurturing < 0.3, the agent attempts to tell a joke or show a pleasing visualization to "fix the world."
Immediate Next Step
To start, you need to ensure your environment handles the camera and the LLM.
Do you want me to provide the initial Python environment setup commands and the MotiveSystem class code to feed into Claude Code first?



